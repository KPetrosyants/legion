new SlimSelect({
    select: "#offers",
    settings: {
        showSearch: false, // used in example
    },
});
new SlimSelect({
    select: "#availability",
    settings: {
        showSearch: false, // used in example
    },
});
new SlimSelect({
    select: "#view-filter",
    settings: {
        showSearch: false, // used in example
    },
});
